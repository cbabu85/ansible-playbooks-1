---
# ID without suffix (e.g. 'virginia' not 'virginia.edu')
inst_id: cd3ef
inst_suffix: edu

inst_groups:
    - "{{inst_id}}.{{inst_id_suffix}}.users"
    - "test.{{inst_id}}.{{inst_id_suffix}}.users"
    - "staging.{{inst_id}}.{{inst_id_suffix}}.users"

aws_preservation_service_users:
    - apt.go.demo
    - apt.go.staging
    - apt.go.production

aws_preservation_group: 'apt_preservation_services'
